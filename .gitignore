# agent / machine-specific config (preview launch config, session data)
.claude/

# unrelated MCP tooling scraps
mcps/

# agent terminal logs
terminals/

# misc
*.log
.DS_Store

# === Godot ===
# Imported asset caches (regenerated from source by the editor on open).
.godot/
# Per-user editor state.
editor_layout.cfg

# === Legacy (web build, archived) ===
legacy/node_modules/
legacy/.serve-cache/
